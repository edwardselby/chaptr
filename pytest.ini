[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# Disable Faker plugin - not used in tests, adds ~0.2s per test for locale initialization
addopts = -p no:faker
markers =
    integration: Complex integration tests requiring real MongoDB (deselect with '-m "not integration"')
